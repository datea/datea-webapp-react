
-> format-geocoded-address : quitar el codigo postal de las direcciones.



DATA:
  - hydrate campaign with subtags Map with tag as key
  - lastLoggedOutURL con nuevo router



crear componentes:

Basicos:

<Apoyar />
  -> model

<Seguir />
  -> model

<Compartir />
  -> model ?
  -> url

<AuthorInfo />
  -> author model
  -> subtitle

<Dateo />
  -> model
  -> presentation: 'teaser', 'detail', 'image'
  -> onMapPinClick
  -> onOpenClick
  -> onEditClick

  <UrlView />
    -> url

  <ImageGrid />
    -> image objects

  <FileView />
    -> File Objects

  -> transladar todas las urls a al texto del dateo, abajo


<Mapa/>
  -> dateos
  -> kmls
  -> onPointSelect
  -> getPointColors ?
  -> zoomToDateo ?
  -> zoom
  -> center
  -> fitBounds ?

<MapInput />
  -> onPointSet
  -> onPointDelete
  -> center
  -> zoom
  -> enable


Mapeo:
  <Mapping>
    <SubTags />
    <MappingView>
      -> isMobile
      -> dateos
      -> type: 'campaign, tag'
      -> model: campaign or tag

    <MapControl />
      -> type: 'campaign', 'tag'
      -> dateos
      -> query
      -> filters
